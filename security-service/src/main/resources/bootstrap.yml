server:
  port: ${PORT:9999}

spring:
  application:
    name: security-service
  logging:
    level:
      org.springframework.security: WARN

  cloud:
    config:
     uri: ${CONFIG_SERVER_URL}
     fail-fast: true