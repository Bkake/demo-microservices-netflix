server:
  port: 8282

spring:
  application:
    name: author-service

  datasource:
    url: jdbc:h2:mem:auhtor-db
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  cloud:
    config:
      uri: http://config-service:8888